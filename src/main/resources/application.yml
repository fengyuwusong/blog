spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 80